import{useEffect,useState}from"react";import{Link}from"react-router-dom";import{api}from"../api";export default function Tests(){let[x,setX]=useState([]);useEffect(()=>{api.get("/tests").then(r=>setX(r.data))},[]);return <main className="container"><h1>Mock Tests 📝</h1><div className="grid">{x.map(t=><article className="card" key={t._id}><h2>{t.title}</h2><p>{t.category} • {t.duration} minutes</p><p>{t.questions.length} questions</p><Link to={`/exam/${t._id}`}><button>Start Test</button></Link></article>)}</div></main>}