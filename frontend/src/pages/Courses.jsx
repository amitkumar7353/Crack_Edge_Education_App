import{useEffect,useState}from"react";import{api}from"../api";export default function Courses(){let[x,setX]=useState([]);useEffect(()=>{api.get("/courses").then(r=>setX(r.data))},[]);return <main className="container"><h1>Courses 📚</h1><div className="grid">{x.map(c=><article className="card" key={c._id}><h2>{c.title}</h2><p>{c.description}</p><b>{c.category}</b></article>)}</div></main>}