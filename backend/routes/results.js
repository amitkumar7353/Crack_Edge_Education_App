const r=require("express").Router(),Result=require("../models/Result"),Test=require("../models/Test"),auth=require("../middleware/authMiddleware"); r.post("/:testId",auth,async(req,res)=>{let t=await Test.findById(req.params.testId);if(!t)return res.status(404).json({message:"Test not found"});let score=t.questions.reduce((s,q,i)=>s+(req.body.answers?.[i]===q.correctAnswer?1:0),0);let result=await Result.create({student:req.user.id,test:t._id,score,totalQuestions:t.questions.length});res.json({message:"Submitted",result})}); r.get("/",auth,async(req,res)=>res.json(await Result.find({student:req.user.id}).populate("test","title category").sort({createdAt:-1}))); module.exports=r;